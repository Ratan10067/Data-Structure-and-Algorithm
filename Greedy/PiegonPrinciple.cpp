// this is a set problem which we can find subset whose sum is equal to some constant which given in problemm statement.

#include <bits/stdc++.h>
using namespace std;

using lli = long long int;
#define mod 1000000007
#define IOS ios_base::sync_with_stdio(false)
#define pi 3.14
#define F first
#define S second
#define MP make_pair
int gcd(int a, int b)
{
    if (a == 0)
        return b;
    return gcd(b % a, a);
}
signed main()
{
    
    return 0;
}